'use client';

interface PhotoToDesignPromoProps {
  isPro: boolean;
  onClose: () => void;
}

export function PhotoToDesignPromo({ isPro, onClose }: PhotoToDesignPromoProps) {
  return null;
}

/** @deprecated Use PhotoToDesignPromo */
export const PhotoToLayoutPromo = PhotoToDesignPromo;
