'use client';

/**
 * ContextMenu — placeholder.
 *
 * The previous implementation referenced APIs and color tokens that no
 * longer exist (legacy nested COLORS shape, removed canvasStore methods,
 * direct `fabric.*` namespace usage). Right-click context-menu actions
 * are currently surfaced through the Command Palette + selection actions
 * hooks, so this component is intentionally stubbed pending a rewrite.
 *
 * Keeping the file (rather than deleting it) preserves the import path
 * for any downstream code that re-introduces a context menu later.
 */

export function ContextMenu(): null {
  return null;
}
