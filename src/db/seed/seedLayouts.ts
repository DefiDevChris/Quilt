/**
 * Hardcoded templates have been removed.
 * The system now purely relies on DB-persisted patterns.
 * * Keep this file structure intact for future data ingestion/seeding scripts 
 * targeting the layoutTemplates schema.
 */
export const seedTemplates = [];
