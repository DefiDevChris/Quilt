/**
 * Next.js instrumentation hook — runs once at server startup.
 */

export async function register() {
  // Server-side initialization placeholder
  // Secrets and env validation can be added here when needed
}
