'use client';

import { PhotoToDesignWizard } from '@/components/photo-to-design/PhotoToDesignWizard';

export default function PhotoToDesignPage() {
  return (
    <main className="h-screen">
      <PhotoToDesignWizard />
    </main>
  );
}
