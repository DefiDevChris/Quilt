--- route.ts
+++ route.ts
@@ -82,7 +82,7 @@
       if (dbProject.version > currentVersion) {
         return NextResponse.json(
           {
-            error: 'VERSION_CONFLICT',
+            error: 'NOT_FOUND',
             message: 'Project has been modified in another session. Please reload to see the latest changes.',
           },
           { status: 409 }
@@ -103,7 +103,7 @@
         // Include worktables if they exist in the payload
         ...(body.worktables
           ? {
-              worktables: body.worktables as Record<string, unknown>,
+              worktables: body.worktables as unknown as Record<string, unknown>,
             }
           : {}),
         updatedAt: new Date(),
