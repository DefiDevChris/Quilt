/**
 * Geometry Type Definitions
 * Canonical point and shape interfaces.
 */

export interface Point {
  x: number;
  y: number;
}

/** Alias for backward compatibility */
export type Point2D = Point;
